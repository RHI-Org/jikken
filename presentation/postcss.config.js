// Empty on purpose — stops Vite from walking up to folio's postcss.config.js
// (jikken is nested inside the folio checkout locally, but is its own repo
// and deploys independently). No Tailwind in the presentation app yet.
export default {
  plugins: {},
};
